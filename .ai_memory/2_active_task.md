# 当前任务状态

## 正在进行的任务
- [ ] 修复摄像头掉线重连问题，支持自动搜索可用端口
- [ ] 验证自动重连机制

## 任务上下文
用户反馈摄像头在使用过程中容易松动，导致设备路径（如 /dev/video0）发生变化，程序无法自动恢复。现有代码仅尝试重连配置的固定端口。

## 下一步计划
修改 `src/camera_manager.py`，增加扫描可用摄像头设备的功能，并在重连时尝试连接其他可用端口。
